import { ResourceDefinition, ResourceLoaderOptions } from '../types/external';
declare const normalizePath: (path: string, resource: ResourceDefinition, options: ResourceLoaderOptions) => string;
export default normalizePath;
