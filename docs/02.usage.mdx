---
name: Usage
route: '/usage'
---

# Usage

## Create
To init a resource-loader just create a new instance of the resource loader class:
```javascript
const resourceLoader = new ResourceLoader();
```

This will search your whole body for resource elements and load all the resources defined on them.

## Resource Elements
By setting the `data-resources` attribute on any html tag you can create a 
