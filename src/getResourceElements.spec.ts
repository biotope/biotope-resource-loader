import { expect } from 'chai';
import getResourceElements from './getResourceElements';

describe.skip('#getResourceElements', () => {

    it('returns empty array for non existent resource elements', () => {
        expect(false).to.be.true
    });
})
